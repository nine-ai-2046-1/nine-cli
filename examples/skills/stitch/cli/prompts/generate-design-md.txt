你以 agent 身份執行以下步驟並回報結果（用廣東話）：
1) 呼叫 Stitch MCP 生成單一 DESIGN.md：
- 使用 API: stitch_generate_screen_from_text(projectId="{projectId}", deviceType="DESKTOP", prompt=<下方 prompt body>)
- prompt body（必須放入）：
  Reference: {source_url}
  Task: 只生成一份完整的 DESIGN.md（Markdown），作為該專案的 canonical design system。絕對禁止生成其他 screens、images 或變體 —— 唔可以生成多過一樣 artefact。
  Requirements for DESIGN.md:
  - Brand & style summary (1–3 段)
  - 顏色 tokens（含 hex 與 role，例如 primary/secondary/surface/error/success）
  - Typography scale（字體、size、weight、line-height）
  - Spacing/grid 建議（base unit）
  - Corner radius、elevation 規則
  - Component rules（header, buttons, cards, forms, footer, nav）
  - Accessibility notes（最少 text size、contrast、tap target）
  Save: 把 DESIGN.md 同時寫入 project path `.stitch/DESIGN.md`，並且把完整 Markdown 放入 stitch 的 output_components（好讓 agent 下載）。
2) 若 step1 成功（回傳 DESIGN.md），agent 需自動：
- 把 output_components 裡嘅 DESIGN.md 存到 local path：
  `generation/current/{projectId}/DESIGN.md`
- 呼叫 stitch_create_design_system_from_design_md（或等效 API）把該 DESIGN.md 註冊為 design system 資產，紀錄回傳的 asset id（assets/{id}）。
3) 最後回報（agent 要以廣東話回覆，並用 emoji 標示狀態）：
- 成功／失敗 狀態（✅ / ❌）
- 若成功，回報三樣資訊：
  1. 本地完整檔案路徑：/Users/xxx/yyy/generation/current/{projectId}/DESIGN.md
  2. Stitch asset id（如果已註冊）：assets/{id}
  3. 簡短 token-map（例：primary: #xxxxxx; headlineFont: Inter; baseSpacing: 4px）
- 若失敗，列出錯誤訊息與建議（例如：MCP timeout、invalid argument、需 user 確認等）。
重要規則（必須遵守）：
- 一次只做一件事：絕對只請求一個 DESIGN.md，不好再同時要求 image 或 pptx。
- 覆寫 design system 或上傳新 asset 前若會影響現有資產（已有 asset id），先向我確認（除非我事先授權自動覆寫）。
- 所有下載自動放 `generation/current/{projectId}/` 的對應子資料夾（files/ code/ screens/），DESIGN.md 放根下。
請開始執行步驟 1–3，執行結果直接用廣東話回報我（要包含上面要求嘅三樣資訊）。多謝 ✅
— agent 應做嘅技術細節（短）
- 呼叫 stitch_generate_screen_from_text，prompt 如上（只要求 DESIGN.md，並要求把 content 放入 output_components）。
- 檢查 outputComponents：找到 design markdown（text / design），寫入 project 的 .stitch/DESIGN.md（或本地 temp），然後把完整 Markdown 存為 local file generation/current/{projectId}/DESIGN.md。
- 註冊資產：呼叫 stitch_create_design_system_from_design_md（或 stitch_upload_design_md + stitch_create_design_system_from_design_md），取得 assets/{id}。
- 最後回覆包含本地路徑、asset id、及 token 摘要。
— 範例最終回覆格式（agent 回你時應該用）
- 「收到 ✅ 已完成：DESIGN.md 已生成及註冊」  
- 本地完整路徑： /Users/xxx/yyy/generation/current/12734585617426837069/DESIGN.md  
- Stitch asset id： assets/a123...  
- Token 摘要： primary=#C8102E; headlineFont=Inter; baseSpacing=4px



